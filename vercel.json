{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "python3 -c \"import os; v=os.environ.get('SCANNER_MIGRATIONS_DATABASE_URL',''); s=os.environ.get('SCANNER_DATABASE_URL',''); print('--- probe: env var availability at build time ---'); print('SCANNER_MIGRATIONS_DATABASE_URL len=', len(v), 'looks_real=', v.startswith('postgresql')); print('SCANNER_DATABASE_URL len=', len(s), 'looks_real=', s.startswith('postgresql')); print('--- end probe ---')\"; npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.py": {
      "maxDuration": 30,
      "excludeFiles": "{.venv/**,.git/**,.event-image-edge-profile/**,.freebuff/**,.vercel/**,node_modules/**,dist/**,dist-server/**,src/**,server/**,lib/**,docs/**,tests/**,alembic/**,GatepassApi/**,loop-engineering/**,**/__pycache__/**,**/*.db,**/*.db-wal,**/*.db-shm,**/*.md}"
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
