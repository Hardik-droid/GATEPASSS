# Runtime dependencies for the Vercel Python Functions under api/.
# Vercel supplies the ASGI server, so uvicorn is not needed here; alembic and
# pytest are build/dev-time only and stay in backend/requirements.txt.
fastapi==0.139.0
sqlalchemy==2.0.51
greenlet==3.5.4
psycopg[binary]==3.3.4
pydantic==2.13.4
python-dotenv==1.2.2
# [crypto] pulls in `cryptography`, which PyJWT requires to verify the EdDSA
# (Ed25519) signatures that Neon Auth's JWKS publishes. Without it every token
# check fails with "Algorithm 'EdDSA' could not be found".
PyJWT[crypto]==2.13.0
qrcode==8.2
pillow==12.3.0
