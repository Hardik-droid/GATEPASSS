# Nixpacks configuration for Railway Node.js Express server
[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build:server"]

[start]
cmd = "node dist-server/index.js"
